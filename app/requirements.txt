# Haylou S40 — Windows control app
# Python 3.10+ on Windows. Install with:  pip install -r requirements.txt

flet>=0.28           # UI (Flutter-based desktop)
# S40 usa Classic BT RFCOMM (não BLE): conexão via socket.AF_BTH nativo do Windows
# Nenhuma lib BT extra necessária — socket e winsdk (abaixo) cobrem discovery+connect
pycaw>=20240210      # Windows volume control
comtypes>=1.2        # COM bridge (pycaw / audio endpoints)
winsdk               # Windows Runtime — "now playing" (SMTC). (winrt>=1.0 was the old name,
                     # incompatible with Python 3.13+; winsdk is the current replacement)
pystray>=0.19        # system tray icon
pillow>=10.0         # tray icon rendering (PIL)
keyboard>=0.13       # global hotkey (Ctrl+Alt+A)
win10toast-click>=0.1.2  # Windows toast notifications (low-battery alert); optional

# Packaging the .exe (optional, only to build a standalone binary):
# pyinstaller>=6.0
